# `services/dis-ui/` — *v1.0*

The DIS frontend application. Single containerized service that hosts the
entire user-facing UI: tenant and ops surfaces. Auth via Customer Master.
Calls one backend: `dis-ui-server`.

**Stack:** TBD. Chosen in build-guide Slice 19 (DIS UI foundation).

**Status:** scaffolding placeholder. Real structure lands in Slice 19.
