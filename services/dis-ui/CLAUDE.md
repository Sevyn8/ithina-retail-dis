# services/dis-ui — Claude Code Context

Loaded when Claude Code works in `services/dis-ui/`. Service-specific rules;
root `CLAUDE.md` applies first.

**Status:** placeholder. The frontend stack, build tooling, and folder
layout are decided in build-guide Slice 19 (DIS UI foundation). Until then,
this directory only reserves the service slot.
