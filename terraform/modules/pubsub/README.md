# Module: pubsub

The DIS topics and a default pull subscription per topic. Confirm the topic set against contracts/pubsub in the app repo. Refine subscriptions (push vs pull, ack deadlines, dead-letter policies) per consumer when the services are deployed.
